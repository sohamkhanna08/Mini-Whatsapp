<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit the Message</title>
</head>
<body>
    <h2>Edit the Message</h2>
    <p>Chat Id : <%= editChat._id %></p>
    <p>This chat was sent from <i><%= editChat.from %></i> to <i><%= editChat.to %></i></p>
    <p>Time of the message : <%= editChat.created_at.toString().split(" ")[4]%></p>
    <p>Day of the message : <%= editChat.created_at.toString().split(" ")[0]%></p>
    <p>Date of the message : <%= editChat.created_at.toString().split(" ").slice(1,4).join("-")%></p><hr>
    <form method="post" action="/chats/<%= editChat._id %>?_method=PUT">
        <textarea name="msg" rows="5" cols="30"><%= editChat.msg %></textarea><br><br>
        <button type="submit">Confirm the Edit</button>
    </form>
    
</body>
</html>